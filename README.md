
  # Prototype transitions setup

  This is a code bundle for Prototype transitions setup. The original project is available at https://www.figma.com/design/nqQKsE6CAY3ljUiHrtrf0X/Prototype-transitions-setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  